{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "makeit",
            "type": "shell",
            "command": "./makeit -build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "./makeit -clear -reconf"
        }
        {
            "label": "package",
            "type": "shell",
            "command": "./makeit -ssm"
        }
    ]
}